apiVersion: v1
kind: Service
metadata:
  name: nestjs-clean-architecture
  labels:
    app: nestjs-clean-architecture
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: nestjs-clean-architecture